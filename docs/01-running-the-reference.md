# Running the Reference

Use Node.js 24 or newer, then run:

```bash
npm start
```

The command executes `index.js`, which prints each built-in category in a predictable order. No package installation is necessary because the project has no third-party dependencies.

On Windows systems where PowerShell blocks the `npm.ps1` shim, run `npm.cmd start` instead.
